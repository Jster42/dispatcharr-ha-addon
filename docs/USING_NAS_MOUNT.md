# Using a NAS mount with Dispatcharr

This page is superseded by [MOUNTING_NAS.md](MOUNTING_NAS.md).

Use **Settings → System → Storage → Add network storage** with usage **Media**. Then set this add-on's `nas_path` to `/media/<share-name>` and enable `nas_symlinks`.
